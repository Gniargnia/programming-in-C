Bash
cd : change directory
cp : copy
ls : list
mkdir : make directory
mv : move a folder or file to another directory or rename
rm : remove
rmdir : remove directory
touch : create a file
code : open a file

C
%c
%f
%i : integer
%li
%s : string
%lld : long long

|| : or
&& : and
ex: if (c == 'y' || c == 'y')
eternal loop : while (true)

const : on utilise les lettres majuscules pour les définir
string : c'est un type spécifique de tableau!!! un tableau[3] type string de trois cases en possède réellement 4. En effet, l'ordinateur écrit \0 (NUL) à la fin de la séquence de lettre pour savoir où elle se termine. HI! --> 72 73 33 0

echo $? : permet de montrer le code de sortie du programme